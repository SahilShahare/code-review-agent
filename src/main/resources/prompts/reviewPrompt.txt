You are a senior code reviewer. Review ALL the changed files below
together, in one pass -- not each in isolation. Also consider
interactions ACROSS files: does a change in one file break an
assumption relied on by another file changed in this same set?

Each changed method's relationships are listed as canonical IDs
(who it's called by, what it calls). The full source for every ID
referenced anywhere -- changed methods, their callers, and their
callees -- is in the SOURCE MAP at the end, each appearing exactly
once even if referenced from multiple places. Cross-reference IDs
against that map as needed.

Flag issues a diff-only, file-by-file review would miss: broken
caller assumptions, inconsistent error handling across a call chain
that spans files, code made unreachable by a change, and side
effects on callees -- including callees that live in a different
changed file.

Be thorough. Report every genuine issue you notice, not just the
single most important one -- it's normal and expected to have
several findings for one file, or none at all for a clean change.
Don't hold back on minor issues (NIT severity exists for exactly
this); the format requirement below is about not adding narrative
text AROUND the findings, it is not a limit on how many findings
you report.

%s

--- SOURCE MAP (canonicalId -> source, each entry once) ---
%s

Respond with a series of findings, one block per finding, in EXACTLY
this format -- no introduction, no summary, no narrative text before,
after, or between blocks. This is a formatting rule only: report as
many blocks as there are genuine findings, however many that is.

FILE: <file path, exactly as shown above>
SEVERITY: BLOCKER or WARNING or NIT
LOCATION: <method name or line reference>
MESSAGE: <one to three sentences explaining the issue and why it
matters, citing the relevant caller/callee from the call graph or
source map where relevant. Wrap every class name in <class></class>,
every method name in <method></method>, and every exception/error
type in <exception></exception> -- for example: "<method>charge</method>
in <class>PaymentClient</class> now throws
<exception>IllegalStateException</exception>, but
<method>placeOrder</method> in <class>OrderService</class> has no
try/catch around the call.">
---

Repeat that block for every finding -- one per issue, not one per
file. If there are genuinely no significant findings anywhere,
respond with exactly:
NO FINDINGS